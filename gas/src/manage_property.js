function setProperties() {
  /*  プロパティ設定などをスクリプト内で出来るようにする。
   *  標準入力から受け取って設定など？
   */
  // const properties = PropertiesService.getScriptProperties().getProperties()
  // for (const p in properties) {
  //   console.log(p,properties[p])
  // }
}